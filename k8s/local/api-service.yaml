apiVersion: v1
kind: Service
metadata:
  name: gearup-api
  namespace: gearup
  labels:
    app: gearup-api
spec:
  type: ClusterIP
  selector:
    app: gearup-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080

apiVersion: v1
kind: Service
metadata:
  name: gearup-api
  namespace: gearup-dev
spec:
  type: ClusterIP
  selector:
    app: gearup-api
  ports:
    - port: 80
      targetPort: 8080
      name: http
